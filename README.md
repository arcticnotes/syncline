
# Sync-Line

A utility library that spawns a subprocess, sends lines to its standard input, and reads lines back from the standard
output synchronously, without `Promise`s, callbacks, or `async`/`await`.
